import { getMyInfo, logout, editMyInfo } from 'api/customer/user';
import { useEffect } from 'react';

const MyInfoPage = () => {
  useEffect(() => {});
  return <div>손님 내정보 메인페이지</div>;
};

export default MyInfoPage;
